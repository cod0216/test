#include <stdio.h>
#include <stdlib.h>

int number(int a, int b)
{
	int i
	for(i = 0; i < 10; i++)
	{
		k = k + a + b;
		j = j + k +a;
	}

	return j;
}

int main(){
	int value = 4;
	int key = 10;
	int namba;
	namba =	number(value, key);

	printf("%d \n", namba);

	return 0;
}
